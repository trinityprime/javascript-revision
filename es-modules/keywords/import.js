// es-modules => separate and organise code (split code into multiple files)

import printCharacter, { exportCharacter } from "./exporting.js";

console.log("Stats:");
printCharacter();
console.log(exportCharacter);
